/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  ...nextConfig,
  // images: {
  //   domains: ["thisis-images.scdn.co", "i.scdn.co", "mosaic.scdn.co"],
  // },
};
